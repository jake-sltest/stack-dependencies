- name: Stack Dependencies Demo
  hosts: localhost
  connection: local
  become: true
  gather_facts: no
  pre_tasks:
      - name: Display output from ec2 Stack
        ansible.builtin.debug:
          msg: [
            "ANSIBLE: What is the output from the ec2 stack?",
            "{{(lookup('env','ec2Id'))}}"
          ]